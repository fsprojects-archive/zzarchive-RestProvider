 * You'll need Git lfs to get the WorldBank data.
 * If you want to recreate the WorldBank cache, extract `data/worldbank.zip` into `src/SuaveSources/worldbank`

Run servers in the background using

    build RunServers
